{
"datastream_revision_id": "{{ datastream_revision.id }}",
"title": "{{ datastreami18n.title}}",
"description": "{{datastreami18n.description}}",
"allStatus":[
{% for x, y in allowed_status %}
    {"value":"{{ x }}","label":"{{ y }}"}
    {% if not forloop.last %}
        ,
    {%endif %}
{% endfor %}
],
"status": "{{status}}",
"tags": "{{datastream_revision.tags}}",
"sources": "{{datastream_revision.sources}}",
"allCategories":[
{% for category in categories %}
    {"value":"{{ category.category__id }}","label":"{{ category.name }}"}
    {% if not forloop.last %}
        ,
    {%endif %}
{% endfor %}
],
"category": "{{datastream_revision.category}}",
"notes": "{{datastreami18n.notes}}"
}